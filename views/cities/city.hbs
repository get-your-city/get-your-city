<div class="container">
<div class="row justify-content-center">

    <div id="city-card" class="col-xs-10 col-md-8">
   <div class="card text-center shadow-lg p-xs-1 p-md-3 mb-5 bg-white rounded">


        <img src="{{city.imageUrl}}" class="card-img-top img-fluid max-width:100%" alt="">
        
        <div class="card-body">
        
        <div class="row card-title align-items-center justify-content-center">
        <h2 class="col-xs-6 col-lg-7 text-left" >{{city.name}}</h2>
        <div class="weather col-xs-3 col-lg-2 font-weight-bold pt-2">
        <p>{{weather.temperature}}°C</p>
    <p>{{weather.condition}}</p>
    </div>
    <div class="weather col-xs-3 col-lg-2">
    <img src="{{weather.icon}}" alt="">
    </div>
        
        </div>


    <p id="country" class="text-left font-weight-bold">
    {{city.country}} 
        </p>
        <div id="city-description" class="card-text text-justify">
        <p>{{city.description}}</p>
        </div>
        
<div class="card-body text-center">
    <a href="/cities/{{city._id}}/activities" class="card-link btn btn-info m-1">Activities in {{city.name}}</a> <br>


    <a href="/cities/{{city._id}}/edit" class="btn btn-outline-primary m-1">Edit</a>

    <form action="/cities/{{city._id}}/delete" method="POST">
        <button class="btn btn-outline-secondary m-1">Delete</button>
    </form>
</div>
 </div>
  </div>
   </div>
    </div>
</div>

